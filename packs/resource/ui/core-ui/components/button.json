{
	"namespace": "core_ui_components",
	// Extends the shared base (core_ui_common.control). The base owns the generic
	// control-block decode; the four state faces below SELF-decode their textures at the
	// button's fixed offsets (background [440], hover [1024], pressed [1107], locked
	// [1190]) — the old root-level state decode into property_bag was dead weight
	// (computed bindings never propagated to the children anyway) and is gone.
	// $cell_details_binding_type is REQUIRED here (unlike panel): the per-cell collection
	// index is what routes button.form_button_click to the right form button.
	"button@core_ui_common.control": {
		"type": "button",
		"layer": 50,
		"$type_gate": "(#pre_visible and (#type = 'button'))",
		"$cell_details_binding_type": "collection_details",
		// === Base button functionality (from vanilla_button) ===
		"$focus_id|default": "",
		"$focus_override_down|default": "",
		"$focus_override_up|default": "",
		"$focus_override_left|default": "",
		"$focus_override_right|default": "",
		"focus_identifier": "$focus_id",
		"focus_change_down": "$focus_override_down",
		"focus_change_up": "$focus_override_up",
		"focus_change_left": "$focus_override_left",
		"focus_change_right": "$focus_override_right",
		"focus_enabled": true,
		"focus_magnet_enabled": true,
		"focus_wrap_enabled": true,
		"default_focus_precedence": 0,
		"sound_name": "random.click",
		"sound_volume": 1.0,
		"sound_pitch": 1.0,
		"locked_control": "locked",
		"default_control": "default",
		"hover_control": "hover",
		"pressed_control": "pressed",
		"$default_state|default": false,
		"$hover_state|default": false,
		"$pressed_state|default": false,
		"$locked_state|default": false,
		"button_mappings": [
			{
				"from_button_id": "button.menu_select",
				"to_button_id": "button.form_button_click",
				"mapping_type": "pressed"
			},
			{
				"from_button_id": "button.menu_ok",
				"to_button_id": "button.form_button_click",
				"mapping_type": "focused"
			}
		],
		// The four state faces are ONE shared control (core_ui_common.state_face) — only
		// the skip offset differs per state, passed as a whole expression via $face_skip:
		// background [440], backgroundHover [1024], backgroundPressed [1107],
		// backgroundLocked [1190]. Payload source defaults inside state_face already serve
		// the ActionForm (form_buttons / #form_button_text).
		"controls": [
			{
				"default@core_ui_common.state_face": {
					"$face_skip": "('%.440s' * #payload)",
					"$default_state": true
				}
			},
			{
				"hover@core_ui_common.state_face": {
					"$face_skip": "('%.1024s' * #payload)",
					"$hover_state": true
				}
			},
			{
				"pressed@core_ui_common.state_face": {
					"$face_skip": "('%.1107s' * #payload)",
					"$pressed_state": true,
					"$button_offset|default": [
						0,
						1
					]
				}
			},
			{
				"locked@core_ui_common.state_face": {
					"$face_skip": "('%.1190s' * #payload)"
				}
			}
		]
	}
}
