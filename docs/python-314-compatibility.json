{
  "schemaVersion": 1,
  "profileId": "python-3.14-cs",
  "implementation": "Computer System Python 1.0",
  "claim": "targeting",
  "target": {
    "language": "Python",
    "version": "3.14",
    "reference": "https://docs.python.org/3.14/reference/index.html"
  },
  "commands": {
    "canonical": "python",
    "compatibilityAliases": ["micropython"]
  },
  "executionModel": {
    "compilerTarget": "CS486",
    "processModel": "one-Cs486Process",
    "separatePythonVm": false,
    "hostEscape": false
  },
  "frontendLimits": {
    "lexer": {
      "maxSourceCodeUnits": 512000,
      "maxTokens": 131072,
      "maxIdentifierCodeUnits": 512,
      "maxLiteralCodeUnits": 65536,
      "maxDelimiterNesting": 64,
      "maxIndentationDepth": 64
    },
    "parser": {
      "maxStatements": 16384,
      "maxBlockNesting": 64,
      "maxExpressionNesting": 64,
      "maxParameters": 256,
      "maxArguments": 256,
      "maxItemsPerConstruct": 4096,
      "maxFormattedStringExpressions": 256
    },
    "scopeAnalysis": {
      "maxScopeNesting": 64,
      "maxScopes": 1024,
      "maxSymbolsPerScope": 4096
    }
  },
  "nameBinding": {
    "identifierClasses": "Unicode XID_Start/XID_Continue",
    "identifierNormalization": "NFKC",
    "bindings": ["global", "local", "cell", "free"],
    "declarations": ["global", "nonlocal"],
    "closureStorage": "shared reachable runtime cells",
    "failedSourceImport": "rollback and retry"
  },
  "callBinding": {
    "parameterKinds": [
      "positional-only",
      "positional-or-keyword",
      "keyword-only",
      "variadic-positional",
      "variadic-keyword"
    ],
    "argumentUnpacking": ["iterable", "mapping"],
    "defaultEvaluation": "definition-time left-to-right",
    "callItemEvaluation": "left-to-right",
    "maxExpandedArguments": 4096,
    "chainedComparisons": "short-circuit; each operand at most once"
  },
  "expressionControlFlow": {
    "conditionalEvaluation": "condition first; exactly one result branch",
    "conditionalAssociativity": "right-to-left",
    "lambdaBody": "expression-only",
    "lambdaParameters": "shared five-kind function binder",
    "lambdaExecution": "CS486 call/return with shared closure cells"
  },
  "assignmentExpressionSemantics": {
    "target": "identifier only",
    "rhsEvaluation": "exactly once before store",
    "result": "the same evaluated value",
    "binding": "existing global/local/cell/free scope binding",
    "unparenthesizedContexts": [
      "if test",
      "while test",
      "flexible list/set display item",
      "comprehension result",
      "positional call argument"
    ],
    "restrictedSubexpressions": "require parentheses",
    "comprehensionBinding": "containing scope; iterable forms and iteration-target conflicts rejected"
  },
  "comprehensionSemantics": {
    "containers": ["list", "set", "dictionary"],
    "clauses": "synchronous and asynchronous for/if clauses nested left-to-right",
    "leftmostIterable": "evaluated once in the enclosing scope",
    "implicitScope": "targets and later clauses are nested and do not leak",
    "dictionaryResultEvaluation": "key before value",
    "targets": "identifier and bounded list/tuple destructuring",
    "assignmentExpressions": "bind in the containing non-comprehension scope",
    "generatorExpression": "leftmost iterable and iter() are immediate; elements, filters, and later iterables are lazy in the same implicit scope",
    "setSurface": [
      "explicit/starred display",
      "set() with zero or one iterable",
      "membership",
      "len",
      "equality",
      "deterministic iteration"
    ],
    "setHashing": "bounded canonical primitive/tuple keys with mutable values rejected",
    "defaultCollectionLimit": 4096,
    "excluded": ["class-scope comprehensions", "custom data-model protocols"]
  },
  "classSemantics": {
    "bases": "zero or more class bases evaluated once left-to-right; omitted base uses object",
    "definitionExecution": "bases in the enclosing scope; suite in an isolated class namespace; C3 construction and set-name complete before publication",
    "methodResolutionOrder": "bounded C3 linearization shared by attributes, descriptors, hooks, special methods, patterns, and subclass checks",
    "methodClosure": "class locals are not method closure bindings; enclosing function cells pass through the class frame; functions that reference __class__ or builtin super capture one hidden class cell",
    "classCellFinalization": "initialize to the exact class after C3 and heap admission but before __set_name__; clear it when set-name completion rejects",
    "super": {
      "forms": ["super()", "super(type)", "super(type, instance-or-subclass)"],
      "lookup": "resume after type in the receiver canonical C3 MRO and reuse ordinary managed descriptor binding",
      "reflection": ["__thisclass__", "__self__", "__self_class__"]
    },
    "attributeLookup": "inherited data descriptor, instance namespace, inherited non-data descriptor, then inherited class value",
    "methodBinding": "managed Python functions are non-data descriptors; instance class lookup binds them while an instance-stored function remains unbound",
    "construction": {
      "newSelection": "resolve inherited __new__ through the requested class canonical C3 MRO; an ordinary Python definition is implicitly static",
      "arguments": "pass the requested class followed by the original positional and keyword arguments exactly once",
      "result": "invoke the returned instance type __init__ only when the result is an instance of the requested class or subclass; otherwise return the exact value",
      "objectNew": "object.__new__(cls) validates one class and allocates one bounded heap-accounted bare instance",
      "init": "reuse ordinary managed calls, permit cooperative object.__init__, and require a None return",
      "ownership": "retain requested class, original arguments, and __new__ result across the existing after-call trampoline"
    },
    "builtins": [
      "object",
      "object.__new__",
      "object.__init__",
      "super",
      "isinstance",
      "issubclass"
    ],
    "introspectionAttributes": [
      "class.__name__",
      "class.__base__",
      "class.__bases__",
      "class.__mro__",
      "instance.__class__",
      "super.__thisclass__",
      "super.__self__",
      "super.__self_class__"
    ],
    "defaultNamespaceLimit": 4096,
    "maxMroLength": 64,
    "excluded": [
      "metaclasses",
      "custom generic subscription hooks",
      "__slots__",
      "custom operator protocols",
      "tuple classinfo for isinstance and issubclass"
    ]
  },
  "decoratorSemantics": {
    "targets": ["function", "class"],
    "expressionGrammar": "assignment_expression",
    "expressionEvaluation": "containing scope, top-to-bottom, before function defaults or class bases and suite execution",
    "application": "bottom-to-top through the shared bounded call path",
    "binding": "publish only the final result after every decorator call succeeds",
    "defaultDecoratorLimit": 4096,
    "excluded": []
  },
  "iteratorSemantics": {
    "builtins": [
      "iter(iterable)",
      "iter(callable, sentinel)",
      "next(optional default)"
    ],
    "builtInIterables": ["str", "list", "tuple", "dict", "set"],
    "iteratorIdentity": "iter(iterator) returns the same iterator",
    "exhaustion": "stable; next without a default raises StopIteration",
    "classSpecialLookup": "user-defined __iter__, __next__, and fallback __getitem__ resolve through the class C3 MRO while ignoring instance attributes",
    "iteratorReturn": "user-defined __iter__ may return a built-in iterator, generator, or class instance with callable class-backed __next__",
    "userDefinedConsumers": [
      "iter",
      "next",
      "for",
      "unpacking",
      "starred displays",
      "iterable call expansion",
      "slice replacement",
      "set()",
      "synchronous comprehensions",
      "generator expressions",
      "yield from"
    ],
    "userDefinedExhaustion": "StopIteration ends iteration, next returns its exact optional default, and yield from receives StopIteration.value",
    "materialization": "user iterators and generators resume through bounded CS486 calls; calls, target stores, slice mutation, and result publication wait for iteration plus arity/capacity validation",
    "materializationOwnership": "the iterator, accumulated values, pending operands/arguments/targets, and original physical return slot remain in one reachable continuation",
    "sequenceFallback": {
      "selection": "when class-backed __iter__ is absent, class-backed __getitem__ creates an independent retained cursor; an explicit __iter__ value, including None, takes precedence",
      "indexing": "integer indexes begin at zero and advance only after a successful item result",
      "exhaustion": "IndexError or an escaping StopIteration makes the cursor stably exhausted; other faults propagate without advancing",
      "ownership": "the source instance and current index remain reachable and each item request is an ordinary bounded managed CS486 call"
    },
    "callableSentinel": {
      "evaluation": "callable then sentinel, exactly once at iterator construction",
      "callables": [
        "managed function or lambda",
        "bound method",
        "class",
        "native function or wait",
        "CS486 extension export"
      ],
      "step": "invoke with no arguments through the ordinary bounded CS486 call path",
      "exhaustion": "consume an equal sentinel result or callable-raised StopIteration and remain stably exhausted; another fault propagates without exhausting",
      "ownership": "the callable, sentinel, and exhaustion flag remain in one accounted internal class-backed iterator",
      "equality": "current CS Profile == semantics; custom __eq__ remains deferred"
    },
    "sharedConsumers": [
      "for",
      "unpacking",
      "starred displays",
      "iterable call expansion",
      "slice replacement",
      "set()"
    ],
    "traversal": "built-in next is O(1); user, sequence-fallback, and generator steps use ordinary bounded calls; materialization is O(produced items)",
    "excluded": ["other generator consumers"]
  },
  "generatorSemantics": {
    "syntax": "optional-value yield statements and yield expressions in directly containing def or lambda scopes",
    "call": "bind arguments and return an unstarted generator without executing the body",
    "resumption": "next, for, send, throw, and close use ordinary CS486 call/return targets",
    "yieldExpressionResult": "None from next/for/send(None), or the exact managed value supplied by send(value)",
    "startRestriction": "sending a non-None value to a created generator raises TypeError without consuming it",
    "throw": "raise an exception instance or the bounded legacy type/value/None-traceback form at the suspended yield; caught throws return the next yield and uncaught throws propagate",
    "close": "inject GeneratorExit; return or escaping GeneratorExit succeeds, yielding raises RuntimeError and closes, and any other exception propagates",
    "exceptionHierarchy": "GeneratorExit derives from BaseException and is excluded from Exception handlers",
    "delegation": "yield from evaluates one iterable once, yields lazily, and uses a subgenerator return value or None from built-in exhaustion as its expression result",
    "delegatedMethods": "send, throw, and close forward to generator delegates; a built-in iterator has no send/throw/close methods and preserves Python-observable missing-method behavior",
    "generatorExpressions": "synchronous implicit-scope generators with immediate leftmost iterator acquisition, lazy remaining clauses, and the shared generator protocol",
    "suspendedState": [
      "locals",
      "closure cells",
      "managed value stack",
      "resume target",
      "active exception handlers and handled faults",
      "pending return, jump, fault, and finally continuation"
    ],
    "completion": "return closes the generator; for observes exhaustion; next/send/throw raise StopIteration with the return value; close returns a handled return value",
    "fault": "an unhandled body exception closes before caller routing and an escaping StopIteration becomes RuntimeError",
    "heap": "created and suspended state, active faults and continuations, delegated iterators/generators, plus stored bound send/throw/close methods remain in reachable PythonHeapAccounting",
    "excluded": ["automatic garbage-collection close"]
  },
  "contextManagerSemantics": {
    "syntax": "synchronous with statements with one or more items, optional assignment targets, and parenthesized item lists",
    "acquisition": "evaluate each manager, resolve class-backed __enter__ and __exit__, retain the bound exit, then call enter",
    "nesting": "enter left-to-right and exit right-to-left as nested with statements",
    "targetAssignment": "inside the protected region; assignment failure is passed to exit",
    "normalExit": "pass None, None, None and ignore the exit result",
    "exceptionExit": "pass the stable exception type, exact exception value, and profile traceback None; truthy exit suppresses and false reraises the exact value",
    "controlExit": "return, break, and continue run every entered exit exactly once before completing control flow",
    "generatorSuspension": "bound exits and pending context finalizers survive yield and close through the existing generator state",
    "admission": "preflight the bound receiver plus three explicit exit arguments before enter",
    "heap": "retained bound exits and their receivers remain reachable through PythonHeapAccounting",
    "excluded": [
      "contextlib and generator-based context-manager decorators",
      "descriptor or metaclass customization of special-method lookup",
      "runtime traceback objects"
    ]
  },
  "coroutineSemantics": {
    "syntax": ["async def", "await", "async for", "async with"],
    "call": "bind arguments and return an unstarted coroutine without executing the body",
    "lowLevelDriver": ["send(None)", "throw(exception)", "close()"],
    "awaitables": [
      "native coroutine",
      "class-backed __await__ iterator that completes without an external scheduler yield"
    ],
    "resumption": "ordinary CS486 call/return targets on the existing Cs486Process; no second Python scheduler",
    "completion": "return closes the coroutine and supplies its exact value through StopIteration to the low-level driver",
    "fault": "awaited faults route through the awaiting coroutine handlers and close an escaping coroutine exactly once",
    "reuse": "a completed or closed coroutine rejects another resume",
    "asyncIteration": {
      "acquisition": "class-level __aiter__ returns the asynchronous iterator directly",
      "step": "class-level __anext__ returns a coroutine or async-generator operation which is awaited through the same CS486 process",
      "exhaustion": "StopAsyncIteration ends the loop and is not exposed to the loop body",
      "control": ["break", "continue"]
    },
    "asyncContext": {
      "acquisition": "resolve class-level __aenter__ and __aexit__, then await entry",
      "nesting": "enter left-to-right and await exits exactly once right-to-left",
      "completion": "normal and control completion pass None fault arguments; fault completion supplies the stable type, exact value, and profile traceback None",
      "suppression": "a truthy awaited exit result suppresses the exact active fault"
    },
    "heap": "created coroutine frames, locals, closure cells, handlers, pending control, bound methods, and active protocol owners remain in reachable PythonHeapAccounting",
    "limits": {
      "maxCallDepth": 64,
      "maxCollectionSize": 4096,
      "instructionSlices": "resumable on the outer CS486 scheduler"
    },
    "excluded": [
      "external-scheduler yields from custom __await__ iterators",
      "asyncio event loops, tasks, futures, and host I/O integration"
    ]
  },
  "asyncGeneratorSemantics": {
    "classification": "an async def containing a directly owned yield returns an unstarted async generator instead of a coroutine",
    "protocol": ["__aiter__", "__anext__", "asend", "athrow", "aclose"],
    "operations": "__anext__, asend, athrow, and aclose return single-use awaitables resumed through ordinary CS486 call/return ownership",
    "yield": "suspends the managed frame and completes the current awaitable with the exact yielded value; asend supplies its exact argument as the yield-expression result",
    "completion": "natural return closes the generator and raises StopAsyncIteration to __anext__/asend/athrow while async for consumes exhaustion",
    "fault": "athrow injects at the suspended yield; escaping StopIteration or StopAsyncIteration becomes RuntimeError and every escaping fault closes exactly once",
    "close": "aclose injects GeneratorExit, runs retained finalizers, returns None on normal close, and turns a yielded value during close into RuntimeError",
    "comprehensions": "eager list/set/dictionary async comprehensions await their implicit CS486 coroutine; async generator expressions remain lazy and may contain async for or await",
    "scope": "the leftmost iterable is evaluated once in the enclosing scope; targets remain private and later clauses retain authored evaluation order",
    "heap": "created/suspended frames, operation arguments, active faults, handlers, pending control, and retained owners are reachable through PythonHeapAccounting",
    "limits": "shared collection, call-depth, heap, and outer CS486 instruction-slice limits; no event loop or second scheduler",
    "excluded": [
      "automatic garbage-collection finalization hooks",
      "asyncio tasks, futures, event loops, and host I/O integration"
    ]
  },
  "exceptionGroupSemantics": {
    "types": ["BaseExceptionGroup", "ExceptionGroup"],
    "syntax": "except* with one required exception type or an optional-parentheses exception-type list; ordinary except and except* cannot be mixed in one try",
    "controlRestrictions": ["return", "break", "continue"],
    "construction": "one non-empty bounded exception tree; BaseExceptionGroup automatically derives ExceptionGroup when every leaf derives from Exception",
    "reflection": [
      "type",
      "message",
      "exceptions",
      "args",
      "__cause__",
      "__context__",
      "__notes__",
      "__traceback__"
    ],
    "methods": ["derive", "subgroup", "split"],
    "matching": "recursively preserve the authored tree shape while each except* clause receives only its matching subgroup",
    "ordinaryException": "temporarily wrap for except* matching, then propagate an unmatched ordinary exception without the temporary group",
    "merge": "combine newly raised exceptions with unmatched leaves in deterministic authored-handler order; bare reraises retain the original subgroup identity and tree",
    "callableCondition": "managed Python functions, lambdas, and their managed bound methods run node-by-node through ordinary bounded CS486 calls",
    "suspension": "except* continuation, active subgroup, unmatched leaves, and new faults remain reachable across generator and coroutine suspension",
    "limits": {
      "maxTreeDepth": 64,
      "maxTreeNodes": 4096,
      "maxCallDepth": 64,
      "instructionSlices": "resumable on the outer CS486 scheduler"
    },
    "excluded": [
      "native, class, extension, or scheduler-yielding callable subgroup predicates",
      "runtime traceback objects beyond the profile None/metadata placeholder"
    ]
  },
  "templateStringSemantics": {
    "prefixes": ["t", "T", "tr", "rt", "TR", "RT"],
    "grammar": "shares replacement-field, debug-equals, conversion, escaped-brace, and nested format-field parsing with formatted strings",
    "evaluation": "interpolation values and nested format fields evaluate exactly once from left to right while authored expression text is retained",
    "runtimeModule": "runtime-owned string.templatelib is reserved before guest lookup and never invokes host Python",
    "types": {
      "Template": ["strings", "interpolations", "values"],
      "Interpolation": ["value", "expression", "conversion", "format_spec"]
    },
    "constructors": "Template accepts bounded string/Interpolation parts and Interpolation accepts value, expression, optional conversion, and optional format_spec",
    "iteration": "Template iteration omits empty strings and yields retained strings/Interpolation values in authored order",
    "concatenation": "Template plus Template merges their adjacent boundary strings; every other mixed operand raises TypeError",
    "conversion": ["s", "r", "a"],
    "matching": "Interpolation exposes value, expression, conversion, and format_spec as its positional pattern surface",
    "heap": "templates, interpolation values, reflected tuples, expression text, format text, and iteration state remain in reachable PythonHeapAccounting",
    "limits": {
      "maxInterpolationsPerParse": 256,
      "maxCollectionSize": 4096,
      "maxStringLength": 65536,
      "maxCallDepth": 64,
      "instructionSlices": "resumable on the outer CS486 scheduler"
    },
    "excluded": [
      "custom __format__ dispatch and the complete Python format mini-language",
      "descriptor or metaclass customization of Template and Interpolation"
    ]
  },
  "descriptorSemantics": {
    "instanceLookupOrder": [
      "inherited data descriptor",
      "instance attribute",
      "inherited non-data descriptor",
      "inherited class value"
    ],
    "instanceAccess": "class-owned __get__ receives the descriptor instance, accessed instance, and most-derived owner class",
    "classAccess": "class-owned __get__ receives the descriptor instance, None, and the accessed class",
    "instanceStore": "an inherited class-owned __set__ receives the descriptor instance, accessed instance, and assigned value before any instance-map mutation",
    "instanceDelete": "an inherited class-owned __delete__ or property deleter owns deletion before any instance-map mutation",
    "setName": "each class-body value with inherited __set_name__ is notified once in namespace order before atomic class publication; inherited values are not renotified",
    "boundMethods": ["__self__", "__func__"],
    "intrinsics": {
      "property": "data descriptor with read-only fget/fset/fdel reflection, getter/setter/deleter replacement decorators, class access returning itself, and explicit missing-accessor AttributeError",
      "staticmethod": "non-data descriptor returning the wrapped value unchanged from class or instance access",
      "classmethod": "non-data descriptor binding the most-derived accessed class to a supported callable"
    },
    "heap": "descriptor values, receivers, owner classes, pending stores, set-name snapshots, wrappers, and bound methods remain in reachable PythonHeapAccounting",
    "limits": {
      "maxMroLength": 64,
      "maxCollectionSize": 4096,
      "maxCallDepth": 64,
      "instructionSlices": "resumable on the outer CS486 scheduler"
    },
    "excluded": [
      "metaclasses and __slots__",
      "arbitrary native descriptors and asynchronous descriptor protocol methods"
    ]
  },
  "attributeCustomization": {
    "explicitRead": "inherited __getattribute__ runs for explicit instance reads; inherited __getattr__ runs only after AttributeError, including descriptor AttributeError",
    "explicitWrite": "inherited __setattr__ owns explicit instance writes and may delegate to object.__setattr__",
    "explicitDelete": "inherited __delattr__ owns explicit instance deletion and may delegate to object.__delattr__",
    "builtins": ["getattr", "setattr", "delattr"],
    "getattrDefault": "returned only after the ordinary lookup and inherited __getattr__ both end in AttributeError",
    "implicitSpecialLookup": "bypasses instance __getattribute__ and __getattr__ and resolves on the class C3 MRO",
    "heap": "hook receivers, names, pending values, and getattr defaults remain in reachable PythonHeapAccounting",
    "limits": {
      "maxCallDepth": 64,
      "maxCollectionSize": 4096,
      "instructionSlices": "resumable on the outer CS486 scheduler"
    },
    "excluded": [
      "module-level __getattr__ and __dir__",
      "metaclass attribute customization",
      "arbitrary native or asynchronous attribute hooks"
    ]
  },
  "deletionSemantics": {
    "targets": [
      "name",
      "attribute",
      "list item",
      "dictionary item",
      "list slice",
      "nested target list"
    ],
    "evaluation": "targets and nested target-list elements delete left-to-right; an error preserves earlier deletions and stops later targets",
    "nameScopes": ["global", "local", "cell", "free"],
    "descriptorDelete": "property deleters and inherited data-descriptor __delete__ run before instance-map deletion",
    "limits": {
      "maxItemsPerConstruct": 4096,
      "maxCollectionSize": 4096,
      "instructionSlices": "resumable on the outer CS486 scheduler"
    },
    "excluded": [
      "user-defined __delitem__ subscription and slice deletion",
      "weak-reference finalizers and object-lifetime __del__"
    ]
  },
  "assertSemantics": {
    "debugMode": "always enabled; no CS Profile -O mode",
    "debugBinding": "read-only True",
    "conditionEvaluation": "exactly once",
    "messageEvaluation": "false condition only; exactly once",
    "failure": "AssertionError through the existing exception path"
  },
  "assignmentSemantics": {
    "ordinaryRhsEvaluation": "before all targets",
    "chainedTargets": "left-to-right; RHS exactly once",
    "augmentedTargetEvaluation": "exactly once before RHS",
    "augmentedOperators": [
      "+=",
      "-=",
      "*=",
      "/=",
      "//=",
      "%=",
      "**=",
      "<<=",
      ">>=",
      "&=",
      "^=",
      "|="
    ]
  },
  "unpackingSemantics": {
    "starredDisplays": ["list", "tuple", "set"],
    "dictionaryMappingUnpack": true,
    "displayEvaluation": "left-to-right",
    "dictionaryOverwrite": "later entry wins",
    "destructuring": "nested list/tuple targets",
    "maxStarredTargetsPerLevel": 1,
    "starredTargetValue": "new list",
    "defaultExpandedCollectionLimit": 4096
  },
  "slicingSemantics": {
    "readableBuiltins": ["str", "list", "tuple"],
    "writableBuiltins": ["list"],
    "unicodeTextUnit": "Unicode code point",
    "bounds": "omitted, negative, and oversized integers use Python clipping",
    "steps": "positive and negative; zero raises ValueError",
    "nonIntegerComponentError": "TypeError",
    "assignmentEvaluation": "RHS before object, start, stop, and step",
    "ordinaryListAssignment": "may resize after final-capacity preflight",
    "extendedListAssignment": "replacement length must equal selected length",
    "defaultCollectionLimit": 4096,
    "mutationFailure": "no partial target mutation"
  },
  "numericSemantics": {
    "integerRepresentation": "compact safe integer or exact arbitrary precision",
    "integerLiteralBases": [2, 8, 10, 16],
    "defaultMaxIntegerBits": 262144,
    "minimumMaxIntegerBits": 53,
    "integerOperators": [
      "+",
      "-",
      "*",
      "//",
      "%",
      "**",
      "<<",
      ">>",
      "&",
      "^",
      "|",
      "~"
    ],
    "growthPreflight": ["power", "left-shift"],
    "floatRepresentation": "IEEE 754 binary64 partial"
  },
  "heapAccounting": {
    "physicalOwner": "GuestProcessMemoryGrant backed by GuestRamLedger",
    "managedQuotaOwner": "PythonHeapAccounting",
    "duplicatePhysicalLease": false,
    "measurement": "reachable graph on allocation pressure and observation"
  },
  "hardware": {
    "supportedProfiles": ["CS486DX", "CS486DX2"],
    "unsupportedProfiles": [
      {
        "profile": "CS386SX",
        "commandStatus": 127
      }
    ]
  },
  "patternMatchingSemantics": {
    "softKeywords": ["match", "case", "_"],
    "patterns": [
      "literal and singleton",
      "dotted value",
      "capture and wildcard",
      "OR and AS",
      "fixed and starred sequence",
      "mapping with optional rest",
      "class with positional and keyword attributes"
    ],
    "subjectEvaluation": "exactly once before case selection",
    "caseSelection": "top-to-bottom; first successful pattern with a truthy guard",
    "capturePublication": "all captures publish atomically only after the complete pattern succeeds",
    "failedPattern": "publishes no captures, including failed OR alternatives",
    "guardBindings": "published before guard evaluation and retained when the guard is false or faults",
    "orBindings": "every alternative binds the same names; failed alternatives roll back",
    "sequenceSurface": ["list", "tuple"],
    "mappingSurface": "built-in dictionary with bounded key lookup and an independent rest dictionary",
    "classSurface": "C3 multiple-inheritance instances, inherited __match_args__, and ordinary instance/class attribute lookup",
    "classAttributeMiss": "pattern failure; non-AttributeError faults propagate",
    "limits": {
      "maxPatternNodes": 4096,
      "maxPatternNesting": 64,
      "maxItemsPerConstruct": 4096
    },
    "execution": "one bounded managed operation per case with work charged to the CS486 syscall cycle debt",
    "excluded": [
      "custom sequence and mapping protocols",
      "descriptor and metaclass customization"
    ]
  },
  "annotationSemantics": {
    "syntax": [
      "annotated assignment",
      "parameter annotations for every parameter kind",
      "return annotation"
    ],
    "owners": ["module", "class", "function"],
    "evaluation": "deferred until __annotations__ is accessed",
    "ordering": "authored parameter/return or executed assignment order",
    "successfulAccess": "returns and caches one mutable dictionary after owner completion",
    "failedAccess": "propagates the fault without caching so a later access retries",
    "conditionalAssignments": "only executed simple module/class annotations contribute entries",
    "partialModuleAccess": "returns a fresh executed-so-far dictionary without caching",
    "classScope": "class and method annotation scopes may resolve the completed immediately enclosing class namespace",
    "closureScope": "nested function annotations retain bounded enclosing function cells",
    "functionLocals": "classify a simple annotated name as local but never evaluate or publish the annotation",
    "nonSimpleTargets": "evaluate the target and optional RHS normally but never evaluate or publish the annotation",
    "cachePublication": "dictionary capacity and managed-heap admission complete before cache publication",
    "execution": "annotation evaluators use ordinary CS486 managed functions, call depth, faults, slices, and cycle debt",
    "limits": {
      "maxEntriesPerResult": 4096,
      "maxScopesIncludingAnnotationScopes": 1024,
      "maxScopeNesting": 64
    },
    "excluded": [
      "__annotate__ and annotationlib alternate formats",
      "from __future__ import annotations stringization",
      "typing standard-library semantics",
      "writable or deletable annotation descriptors"
    ]
  },
  "typeParameterSemantics": {
    "owners": ["function", "class", "type alias"],
    "kinds": ["TypeVar", "TypeVarTuple", "ParamSpec"],
    "reflection": [
      "__name__",
      "__type_params__",
      "__bound__",
      "__constraints__",
      "__default__",
      "__value__"
    ],
    "scope": "bounded annotation scope; names do not leak into the containing scope",
    "functionDefaultsAndDecorators": "evaluate in the containing scope",
    "genericAnnotationsClassBodiesAndAliases": "retain type-parameter cells",
    "lazyValues": "bounds, constraints, defaults, and alias values evaluate on first access",
    "successfulAccess": "cache after managed-heap admission with stable identity",
    "failedAccess": "publish no cache and retry on later access",
    "missingDefault": "one shared typing.NoDefault-shaped sentinel",
    "execution": "ordinary managed CS486 calls, frames, closures, faults, slices, and cycle debt",
    "limits": {
      "maxParametersPerList": 4096,
      "maxScopesIncludingTypeScopes": 1024,
      "maxScopeNesting": 64
    },
    "excluded": [
      "full typing and annotationlib APIs",
      "evaluate function alternate formats",
      "variance introspection",
      "runtime type checking",
      "writable or deletable reflection attributes"
    ]
  },
  "genericAliasSemantics": {
    "owners": ["generic class", "type alias", "list", "dict", "tuple", "set"],
    "reflection": ["__origin__", "__args__", "__parameters__"],
    "cache": "one stable process-local alias per origin and identity-argument tuple",
    "arguments": "one bounded tuple in authored subscription order with evaluated defaults appended",
    "openParameters": "unique retained type parameters in first nested argument occurrence order",
    "resubscription": "open and nested aliases substitute retained parameters recursively",
    "defaults": "missing ordinary and ParamSpec arguments evaluate lazily through managed CS486 calls, cache success, and retry faults",
    "variadic": "one TypeVarTuple consumes the bounded variable-width argument segment; ParamSpec normalizes list, tuple, empty, or sole-parameter expanded argument lists",
    "call": "type-erased dispatch to the original class or supported built-in collection constructor",
    "runtimeClassChecks": "parameterized aliases raise TypeError as isinstance or issubclass class-info values",
    "execution": "ordinary CS486 calls, frames, faults, slices, cycle debt, and reachable managed-heap accounting",
    "limits": {
      "maxAliasesPerProcess": 4096,
      "maxArgumentsPerAlias": 4096,
      "maxNesting": 64
    },
    "excluded": [
      "full typing and annotationlib APIs",
      "custom __class_getitem__ and metaclass subscription",
      "runtime type enforcement",
      "variance introspection",
      "writable or deletable reflection attributes"
    ]
  },
  "typingRuntimeSemantics": {
    "moduleResolution": "one reserved runtime-owned intrinsic typing namespace that guest files cannot shadow; no host Python, pip, or venv",
    "specialTypes": [
      "Any",
      "Never",
      "NoReturn",
      "Self",
      "LiteralString",
      "NoDefault"
    ],
    "specialForms": [
      "Union",
      "Optional",
      "Literal",
      "Annotated",
      "Callable",
      "ClassVar",
      "Final",
      "Required",
      "NotRequired",
      "ReadOnly",
      "TypeGuard",
      "TypeIs",
      "Unpack",
      "Concatenate"
    ],
    "typeParameterConstructors": ["TypeVar", "ParamSpec", "TypeVarTuple"],
    "reflectionHelpers": ["get_origin", "get_args"],
    "identityHelpers": ["cast", "assert_type", "reveal_type"],
    "unreachableHelper": "assert_never raises AssertionError when reached",
    "optional": "canonical Union origin with the requested argument followed by the stable NoneType token",
    "annotated": "read-only __metadata__ tuple after the first type argument",
    "runtimeEnforcement": false,
    "execution": "ordinary bounded CS486 subscription, call, fault, slice, cache, and reachable managed-heap ownership",
    "limits": {
      "maxAliasesPerProcess": 4096,
      "maxArgumentsPerAlias": 4096,
      "maxNesting": 64
    },
    "excluded": [
      "full static-checker behavior",
      "get_type_hints and ForwardRef evaluation",
      "TypedDict and NamedTuple generation",
      "overload registry",
      "Protocol structural checks and runtime_checkable",
      "annotationlib alternate formats",
      "arbitrary metaclass and __class_getitem__ hooks"
    ]
  },
  "packageSemantics": {
    "forms": [
      "dotted import",
      "absolute from import",
      "explicit-relative from import",
      "aliases",
      "parenthesized from-import lists",
      "module-level wildcard import"
    ],
    "packageKind": "regular source package with __init__.py",
    "searchPrecedence": "package __init__.py before same-name .py module",
    "initialization": "parents before children; each module once per process",
    "dottedBinding": "top-level package without as; resolved leaf with as",
    "metadata": ["__name__", "__package__", "__file__", "__path__"],
    "circularImport": "partially initialized namespace remains observable",
    "failedInitialization": "remove incomplete module and child publication; later import may retry",
    "limits": {
      "maxModulesIncludingMain": 64,
      "maxImportDepth": 16,
      "maxAggregateUtf8SourceBytes": 512000
    },
    "excluded": ["namespace packages", "zip imports", "dynamic import hooks"]
  },
  "importRoots": [
    "script-directory",
    "/lib/python",
    "/usr/lib/computer-system/python"
  ],
  "excludedFacilities": [
    "pip",
    "ensurepip",
    "venv",
    "PyPI and wheel installation",
    "CPython bytecode and pyc files",
    "CPython JIT",
    "CPython free-threading and GIL behavior",
    "CPython subinterpreters",
    "Python C API",
    "CPython native extension modules (.so and .pyd)",
    "host process, filesystem, or network escape"
  ],
  "nativeExtensionPolicy": {
    "format": "versioned CS486OBJ",
    "cpythonAbiCompatible": false,
    "status": "partial"
  },
  "stdlibPolicy": {
    "moduleStatuses": ["compatible", "guest-adapted", "unavailable"],
    "manifestStatus": "planned"
  },
  "featureGroups": [
    { "id": "lexical-analysis-and-grammar", "status": "partial" },
    { "id": "expressions-and-calls", "status": "partial" },
    { "id": "simple-statements", "status": "partial" },
    { "id": "compound-statements", "status": "partial" },
    { "id": "execution-model", "status": "partial" },
    { "id": "data-model-and-classes", "status": "partial" },
    {
      "id": "built-in-functions-types-and-exceptions",
      "status": "partial"
    },
    { "id": "imports-and-packages", "status": "partial" },
    {
      "id": "iterators-generators-and-context-managers",
      "status": "partial"
    },
    { "id": "coroutines-and-async-protocols", "status": "partial" },
    { "id": "structural-pattern-matching", "status": "partial" },
    {
      "id": "typing-annotations-and-type-parameters",
      "status": "partial"
    },
    { "id": "exceptions-and-exception-groups", "status": "partial" },
    { "id": "unicode-and-text", "status": "partial" },
    { "id": "standard-library", "status": "partial" }
  ],
  "claimGate": [
    "all feature groups are compatible or explicitly guest-adapted",
    "the standard-library module manifest is complete",
    "host conformance and resource-bound tests pass",
    "the complete npm run validate host gate passes",
    "real Bedrock Dedicated Server verification passes",
    "the published manual is verified in a real browser"
  ]
}
